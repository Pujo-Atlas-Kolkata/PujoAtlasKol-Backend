version: '3.8'

services:
  my-postgres:
    image: postgres
    environment:
      POSTGRES_PASSWORD: mypassword
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    depends_on:
      - my-postgres

